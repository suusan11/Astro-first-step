---
import Layout from '../../layouts/Layout.astro';
import PageTitle from '../../components/PageTitle.astro';
import FormButton from '../../components/FormButton.astro';

const metas = {
	title: "CONTACT",
	discription: "問い合わせ(カスタムフィールド)のページ🍓",
	dir: '/contact',
    path: '../'
}

//reCAPTCHA function
// function onSubmit(token) {
//     var form = document.getElementById('astro-form')as HTMLFormElement;
//     if(form.reportValidity()) {
//     form.submit();
//     }
// }
---
<Layout title={metas.title} description={metas.discription}, dir={metas.dir}, path={metas.path}, menuBgBlue={false}>
    <section class="l-second-page l-bg-blue">
        <div class="l-container">
            <PageTitle text="お問い合わせ"  isWhite={true} />
            <form action="https://ssgform.com/s/KgbqKFobDoKy" method="POST" id="astro-form">
                <dl>
                    <dt><label class="required">メールアドレス</label></dt>
                    <dd>
                        <input name="メールアドレス" required="required" type="email">
                    </dd>
                    <dt><label>コメント</label></dt>
                    <dd>
                        <textarea name="コメント"></textarea>
                    </dd>
                </dl>
                <FormButton type="submit" text="送信する"/>
            </form>
        </div>
    </section>
</Layout>

<style lang="scss">
    @use "../../scss/foundation/rem" as r;
    form {
        background-color: var(--color-white);
        border-radius: 10px;
        padding: 1.8rem 1rem;
        dl {
            margin-bottom: 2rem;
            dt {
                .required {
                    &::after {
                        content: '*';
                        display: inline-block;
                        color: #FF0000;
                        font-size: r.f-rem(10);
                        font-weight: bold;
                        vertical-align: top;
                    }
                }
            }
            dd {
                margin-bottom: 1rem;
                input[type=email],
                textarea {
                    display: block;
                    width: 100%;
                    height: 45px;
                    border: none;
                    background-color: var(--color-gray);
                    border-radius: 5px;
                    font-family: var(--font-jp);
                    font-size: r.f-rem(18);
                    letter-spacing: 0.1rem;
                    padding: 1.5rem 1rem;
                    -moz-appearance: none;
                    -webkit-appearance: none;
                    appearance: none;
                }
                textarea {
                    min-height: 200px;
                }
            }
        }
    }
</style>

<!-- 専用スクリプトの読み込み必要 -->
<!-- <script is:inline src="https://www.google.com/recaptcha/api.js"></script> -->