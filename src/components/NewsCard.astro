---
interface Props {
    thumbnail: string | any,
    alt: string,
	date: string,
	title: string,
    excerpt: string
}

const { thumbnail, alt, date, title, excerpt } = Astro.props;

import { Image } from 'astro:assets';
---

<a href="" class="news__card">
    <p class="thumbnail"><Image src={thumbnail} alt={alt} /></p>
    <p class="date">{date}</p>
    <p class="title">{title}</p>
    <p class="excerpt">{excerpt}</p>
</a>

<style lang="scss">
    @use "../scss/foundation/rem" as r;

    .news__card {
        display: block;
        text-align: left;
        padding-bottom: 3rem;
        .thumbnail {
            width: 100%;
            overflow: hidden;
            img {
                aspect-ratio: 16/9;
                transform: scale(1);
                transition: transform 0.8s cubic-bezier(.76,0,.24,1);
            }
        }
        .date {
            font-size: r.f-rem(12);
        }
        .title {
            font-size: r.f-rem(16);
            font-weight: 500;
        }
        .excerpt {
            font-size: r.f-rem(16);
            font-weight: 300;
            &::after {
                content: '...';
                display: inline-block;
                font-size: r.f-rem(16);
            }
        }
        @media (any-hover: hover) {
            &:hover .thumbnail img {
                transform: scale(1.2);
            }
        }
    }
</style>